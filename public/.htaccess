<IfModule mod_rewrite.c>
RewriteEngine On
RewriteBase /

# Disable Apache automatic directory slash (DirectorySlash)
DirectorySlash Off

# Força HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# Remove barra final de URLs (exceto root) — 301 redirect
RewriteCond %{REQUEST_URI} ^(.+)/$
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^(.+)/$ https://%{HTTP_HOST}/$1 [L,R=301]

# Excluir arquivos estáticos existentes
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Servir index.html de diretório correspondente internamente (sem redirect)
RewriteCond %{DOCUMENT_ROOT}/$1/index.html -f
RewriteRule ^([^/]+(?:/[^/]+)*)$ /$1/index.html [L]

# Arquivos inexistentes com extensão não devem cair no HTML da home
RewriteCond %{REQUEST_URI} \.[A-Za-z0-9]{2,5}$
RewriteRule ^ - [R=404,L]

# URLs sem página física vão para a home, sem tentar servir /src/*.tsx como HTML
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
</IfModule>

# Cache Control
<IfModule mod_expires.c>
ExpiresActive On
ExpiresByType text/html "access plus 1 hour"
ExpiresByType text/css "access plus 1 month"
ExpiresByType application/javascript "access plus 1 month"
ExpiresByType image/png "access plus 1 year"
ExpiresByType image/jpg "access plus 1 year"
ExpiresByType image/jpeg "access plus 1 year"
ExpiresByType image/gif "access plus 1 year"
ExpiresByType image/svg+xml "access plus 1 year"
ExpiresByType image/webp "access plus 1 year"
ExpiresByType font/woff2 "access plus 1 year"
</IfModule>

<IfModule mod_deflate.c>
AddOutputFilterByType DEFLATE text/html text/css application/javascript application/json
</IfModule>
